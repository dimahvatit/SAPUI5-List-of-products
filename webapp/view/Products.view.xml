<mvc:View
    controllerName="my_cat_list.controller.Products"
    xmlns:grid="sap.ui.layout.cssgrid"
    xmlns:mvc="sap.ui.core.mvc"
    xmlns:core="sap.ui.core"
    xmlns:f="sap.f"
    xmlns="sap.m"
    displayBlock="true">
    <Page
        title="{i18n>detialHeader}"
        showNavButton="true"
        navButtonPress="onNavBack">
        <f:GridList
            id="gridList"
            class="sapUiResponsiveContentPadding"
            items="{category>Products}">
            <f:customLayout>
                <grid:GridBasicLayout
                    gridTemplateColumns="repeat(auto-fit, minmax(16rem, 1fr))"
                    gridAutoRows="minmax(4rem, 6rem)"
                    gridGap="1rem"
                    gridRowGap="2rem"/>
            </f:customLayout>

            <f:headerToolbar>
                <Toolbar id="prods-toolbar">
                    <Title text="{category>CategoryName}"/>
                    <ToolbarSpacer/>
                    <SearchField width="30%" placeholder="{i18n>searchPlaceholder}" liveChange=".onFilterProducts">
                        <layoutData>
                            <OverflowToolbarLayoutData
                                shrinkable="true" />
                        </layoutData>
                    </SearchField>
                </Toolbar>
            </f:headerToolbar>

            <core:Fragment fragmentName="my_cat_list.fragments.ProductItem" type="XML" />
        </f:GridList>
    </Page>
</mvc:View>